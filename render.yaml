services:
  - type: web
    name: yt2mp3-converter
    env: node
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    systemDeps:
      - python3
      - ffmpeg